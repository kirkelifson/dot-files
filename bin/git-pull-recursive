#!/bin/bash
# Recursively updates (git-fetch) every directory from current
for x in `find ./ -name .git -type d`;
do
    name=`echo $x | sed 's/[./]*//' | sed 's/\/.git//'`
    echo "Updating: $name" && (cd $x/.. && git pull)
done
